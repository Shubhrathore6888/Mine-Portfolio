import {projects} from "@/app/data/portfolio";
export default function ProjectsSection(){const items=[projects[6],projects[0],projects[1],projects[5],projects[3],projects[4],projects[8]];return <section id="work" className="section"><div className="wrap"><p className="kicker">Selected Work</p><div className="projects">{items.map(p=><a className="project" key={p.title} href={p.live||p.github} target={p.live||p.github?"_blank":undefined} rel="noreferrer"><h3>{p.title}</h3><p className="project-title">{p.category}</p>{p.ongoing&&<p className="project-title">Under development</p>}<p>{p.description}</p><span className="role">— Backend Engineer</span></a>)}</div></div></section>}
